# <Deck> Review Notes (for <author> and <senior reviewer>)

Base reviewed: `<deck>_5.pptx` (<whose version>).
Output: `<deck>_6.pptx` (that deck with the changes below layered on).

## What Changed in `_6`

1. **<change>** (<who asked>). <what, where it sits, what it shows>. Page numbers renumbered N to M.
2. **<replaced figure or example>.** <what was wrong, e.g. not in our data>. Replaced with verified rows from `<file>`: <example, n=...>.

## Flags to Confirm Before It Goes External

- **<claim>.** <why it cannot be verified from our data; what would confirm or soften it>.
- **<scope of a chart>.** <what the chart covers and does not>.

## Comments Already Handled in `_5` (No Action)

<comma-separated list of reviewer points already reflected>.

## Slides You Can Port Independently

| Slide in `_6` | Replaces or adds | Depends on |
|---|---|---|
| 7 | adds after 6 | nothing |
